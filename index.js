const { useCounter } = require("./src/useCounter");
const { useFetch } = require("./src/useFetch");
const { useForm } = require("./src/useForm");

module.exports = {
    useCounter,
    useFetch,
    useForm
}